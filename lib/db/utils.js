import { collection, doc } from "firebase/firestore";
// import { getBrandsRef } from "./brands";

export async function getCollectionRef(uid, brandId, collectionName) {
  // const brandsRef = await getBrandsRef(uid);
  // if (!brandsRef) throw new Error(`${uid}  not found`);
  // const brandDocRef = doc(brandsRef, brandId);
  // if (!brandDocRef) throw new Error(`${brandId} not found`);
  // return await collection(brandDocRef, collectionName);
}
